import express from 'express';

const bookingRouter = express.Router();

bookingRouter.post('/book',);
bookingRouter.get('/viewMyBookings',);
bookingRouter.post('/viewAllBookings',)

export default bookingRouter;