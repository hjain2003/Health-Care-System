import express from 'express';

const stockRouter = express.Router();

stockRouter.put('/editCount',);
stockRouter.post('/addInfo',);
stockRouter.get('/viewStock',);

export default stockRouter;